# solutions
```
// access through ssl command
// use password to log in

// show all files permission and record
ls -l

// change to rootauthority
sudo -l

// There you will see /bin/emacs. Then run the command
sudo /bin/emacs

// open emacs
// press Alt + X and type shell 

cat flag.txt

```
